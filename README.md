# Student Performance Prediction

An end-to-end machine-learning project that predicts a student's math score from demographic information, lunch type, test preparation, and reading/writing scores.

## Run locally

```bash
pip install -r requirements.txt
python application.py
```

Open `http://localhost:5000` in your browser.